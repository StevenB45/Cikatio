# Exemple de configuration .env pour Cikatio
# Copiez ce fichier en .env et remplissez les valeurs nécessaires

# URL publique de l'application (utilisée dans les liens envoyés par email)
APP_URL=http://localhost:3000

# Expéditeur des emails (nom et adresse affichés dans les mails)
MAIL_FROM="Cikatio <no-reply@votre-domaine.com>"

# Configuration SMTP pour l'envoi d'emails
SMTP_HOST=smtp.votre-fournisseur.com
SMTP_PORT=587
SMTP_USER=adresse@votre-domaine.com
SMTP_PASS=mot-de-passe-ou-app-password

# URL de connexion à la base de données (exemple pour PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/cikatio"

# Astuces :
# - Ne versionnez jamais votre vrai fichier .env (utilisez .env.example pour le partage)
# - Pour Gmail, utilisez un mot de passe d'application (voir la doc Google)
# - Pour d'autres fournisseurs, adaptez les valeurs selon leur documentation
